# Список змін

[English](./CHANGES.md) · **Українська**

Помітні зміни, найновіші згори, згруповані за комітом, що їх увів (хеші
відповідають `git log`). Тривіальні та суто стильові коміти опущено — повна
історія у `git log`.

## 2026-07-10

### Фікс OID підпису для Купини

- `Message.constructSigned` жорстко зашивав старий OID `Dstu4145le` у
  `digestEncryptionAlgorithm` кожного signerInfo, навіть якщо повідомлення
  було геш­оване Купиною. Українські верифікатори відкидали цю невідповідність
  як "Невірний підпис(35)". Тепер OID підбирається під геш через `SIG_FOR_HASH`,
  завжди беручи `-pb`-варіант (`Dstu4145le-Dstu7564-256/384/512-pb`), оскільки
  jkurwa підтримує лише криві в поліноміальному базисі.

### Документація, типи та безпека

- Задокументовано весь `lib/` англійськими JSDoc- та inline-коментарями —
  заголовки модулів, опис кожного експорту й нотатки щодо GF(2ᵐ)-математики,
  ASN.1-схем та DSTU/GOST-специфіки (лише коментарі; поведінка незмінна).
- Ручні TypeScript-декларації для публічного API — b049617.
- Додано `SECURITY.md` і явно задокументовано обмеження покриття специфікацій —
  e67df4b, b687983.
- Додано офіційні тест-вектори ДСТУ 7564 для Купини-384 та Купини-512 — 6fea8a1.

### Підтримка хешу Купина (ДСТУ 7564:2014) — feca770

- Новий адаптер `lib/util/kupyna.js` над `@li0ard/kupyna` — Купина-256 / 384 /
  512, кожна з тегом своєї OID-назви ДСТУ 7564.
- Новий движок вибору `lib/util/hashes.js`: будує мапи за дайджестом і за
  алгоритмом підпису, а хеш `subjectKeyIdentifier` лишає на ГОСТ 34.311.
- `Box` обирає хеш автоматично — за сертифікатом підписанта (`hashForCert`) при
  підписі й за `digestAlgorithm` повідомлення (`hashForMessage`) при перевірці.
  Перевизначається через `new Box({ hashMethod })` або на виклик
  `sign(..., { hash })`; невідоме значення кидає `Box.EHASH`.
- `Message` проносить обраний OID дайджесту в CMS і отримує геттер `digestAlgo`;
  TSP-запити, `SigningCertificateV2` та OCSP-відповіді теж його враховують.
- Зареєстровано OID-и хешів ДСТУ 7564 і OID-и підпису DSTU-4145-з-Купиною у
  `lib/spec/rfc3280.js`.
- Додано `@li0ard/kupyna` як залежність і окремий набір тестів
  (`test/test-kupyna.js`).

### Завантаження під plain-Node ESM і фікси strict-mode — 16d1996

- Додано явні розширення `.js` до всіх відносних імпортів у `lib/`, щоб код
  завантажувався під чистим `node` (раніше резолвили лише vite/esbuild).
- Виправлено присвоєння необ'явленим ідентифікаторам, що кидали `ReferenceError`
  у strict-mode (`lib/util.js`, `models/OcspResponse.js`, `services/cmp.js`,
  `util/packed_xml.js`, `models/Priv.js`, `app/keycoder.js`, `point.js`).
- Переписано `examples/gost-crypt.js` та `gost-decrypt.js` з `require()` на ESM.

### Документація та приклади — 0463fd7

- Переписано README (англ.) і додано український переклад (`README.uk.md`) із
  перемикачем мов.
- Додано запускні приклади: `sign` / `verify` / `kupyna` (низькорівневий
  `Message`) та `box-sign` / `box-verify` / `box-encrypt` (високорівневий `Box`),
  плюс індекс `examples/README.md`.

### Інструментарій, CI та залежності

- Оновлено eslint/prettier, додано lint-скрипт і крок лінту в CI — c0f7f08.
- Виправлено npm-метадані пакування та процес публікації — ade69ba.
- Замінено Travis CI на GitHub Actions, прибрано codecov — a7d15d7.
- Запінено `asn1.js` до коміту, звужено діапазони `bn.js` / `buffer` — cc91b19.
- Пропатчено критичну вразливість `vitest` через `npm audit fix` (dev-залежність)
  — 0c96933.
- Перегенеровано lockfile, прибравши застарілі jest/babel-пакети — e22f838.

## 2026-07-09

### Line-ending-safe фікстури та стійкий парсинг store — ab5b1f8

- Додано `.gitattributes` (PEM → LF, DER/PKCS#7 → binary), щоб `core.autocrlf`
  більше не псував побайтові тестові фікстури при checkout на Windows.
- `lib/util/pem.js` `from_pem` прибирає CR перед base64-декодуванням, толеруючи
  CRLF у PEM.
- `Priv.from_protected` більше не дозволяє пізнішому парсеру затерти попередній
  вдалий результат — виправлено завантаження PBES2-ключів.

## 2026-05

### Криві та контейнери ДСТУ 4145

- Додано стандартну криву PB_191 — 0e20017.
- Виправлено розгортання базової точки PB_191 — 8033a1e.
- Завантаження PFX / PKCS#12 контейнерів з альтернативних байтових зсувів —
  a4c2a4c, 43dbf34.

## 2025-06

### ES-модулі та збірка

- Переписано весь код з CommonJS на ES-модулі — 541f5ed, da13693.
- Додано esbuild-бандлер і припинено інлайнити залежності з `node_modules`;
  відносні імпорти втратили розширення файлів (пізніше повернуто для plain Node,
  див. 16d1996) — 3d4ef75, 0a0e05b, acc73d2.
- Міграція тест-раннера з jest на vitest і розбиття юніт-тестів — ea3b21b,
  5065f65.
- Ініціалізація ключів `Box` порожнім списком, щоб тести падали менш галасливо —
  472ba01.

## 2024

- Ігнорувати opaque-баги з IIT-ідентифікатором при читанні контейнерів ключів —
  6d567d6.

## 2023

- Індексація приватних ключів за keyid окремо від сертифікатів — 588e709.
- Приведення пароля до рядка при відкритті JKS-контейнерів — 6c176db.
- Виправлено парсер бітів key-usage сертифіката — d7d5f2d.
- Рутинні оновлення залежностей (dependabot).

## 2022

- Розширено `jk.Box` функціональністю отримання сертифікатів (CMP) — 43cf3b3.
- Читати сертифікат із PFX-бага, якщо він присутній — ee0afec.
- Читання PFX з параметрами у стилі PBES2 (Ощадбанк) і виправлення завантаження
  певних PFX-файлів — ae0f047, c59d0ed.
- Підтримка посилання OCSP за keyid у відповідях — da43ef3.
- `jk.guess_parse` тепер повертає список store, якщо знайдено кілька — 5951184.
- Додано роль підписанта «corporate» (director | other) — b2cfedc.
- Зроблено `dke` необов'язковим у параметрах шифрування; виправлено кофактор
  кривої PB_430 — ecc7ced, 0c28f5a.

## 2021

- Завантаження ще одного формату store приватних ключів — 96fd5f9.

## 2020

### Валідація сертифікатів, ЦСК та OCSP

- Завантаження та індексація списку ЦСК і рекурсивна перевірка підписів
  сертифікатів по ланцюжку — 6c4c68d, e39d34b, b606e26.
- Перевірка key-usage сертифіката та відхилення сертифікатів, чия роль не
  збігається — 18c5aaf, ceaa15a.
- OCSP-перевірки: strict за замовчуванням, опортуністичне виявлення респондера,
  верифікація збережених відповідей і вимога, щоб респондер належав тому ж ЦСК,
  що й сертифікат — bc5830c, 9f5c28a, d706b39, ddf2740.
- Кешування результатів верифікації сертифікатів і окремі RDN-індекси для ЦСК і
  сертифікатів — abfb2a8, 5a306c5.
- Опція обмежити бібліотеку лише стандартними кривими — ea3fca0.

### Мітки часу (TSP) і ланцюжок повідомлення

- Розпакування та перевірка TSP-штампів, з окремим перемиканням міток часу
  контенту й підпису — 4b2cc2e, 0a87b20.
- Додавання OCSP-штампів до повідомлень і запис посилань на сертифікати / повного
  ланцюжка на вимогу — f6893b2, 3b3c3cd.

### Ключі та рефакторинг

- Додано `to_pbes2()` для приватних ключів і читання PBES2-ключів без атрибутів
  (один ключ на файл) — 2c0db7d, b99c708.
- Рефакторинг моделей Message і Certificate; винесення завантажувача й фільтра
  ролей — 4da45d2, 7320be3, cf69bc0, 4cccdfd.

## 2019

### Високорівневий Box API

- Побудовано робочий процес `Box`: підпис і перевірка через box-інтерфейс,
  шифрування/розшифрування через DH, `unwrap()`, контейнери clear-data та
  encrypted-transport, від'єднані підписи — 99704eb, ce91a74, b1d0f9d, 33d2520.
- **Зміна API:** `box.pipe()` тепер повертає проміс — a3ff3bb.

### Ключі, сертифікати та криві

- Додано шифрування та серіалізацію ключів у форматі PBES2 (v1.1.0) — 03c2062.
- Перевірка та побудова самопідписаного кореня; перевірка повідомлень,
  підписаних самопідписаним сертифікатом — 968e438, 54a24a5.
- Переписано `curve.js` і клас точки на ES6-класи; `Certificate` переведено на
  ES6-клас — 804970e, 155eb02, b7c163f.
- Виправлено приховану проблему продуктивності в wNAF-множнику точок — 23d94f1.

## 2018

- Парсинг повідомлення з ASN.1 — 691d13f.
- Додано підтримку TSP (міток часу) ціною переведення `Box.pipe()` в async —
  2e02866.
- Парсинг ECDSA-сертифікатів (лише розбір, без повної підтримки) — 920e2fb.
- Відповідність схемі PBKDF2 (додано поле розміру ключа) — 148019d.

## 2017

- Збереження TSP-токена в CMS-повідомлення — 03cb60a.
- Ліниве розбирання сертифікатів — 296d667.
- Використання `msCrypto` при роботі в IE11 — f40ab5d.
- Завантаження keystore зі стандартними кривими; загортання бага з PBES усередині
  — 447d1a2, 2caaf0f.

## 2016

- Байт-у-байт сумісність із сервером податкової звітності — 9ddcccf.

## 2015

- Розпакування документів `PACKED_XML` MeDoc; винесення `js-lzma` в окремий пакет
  — 7740176, 032b5b7.
- Підтримка транспорту USC/QLB і парсинг `ZPOSTTRANSPORTABLE` — d417bd7, 1276613,
  9021149.
- Підтримка іменованих кривих — 7a81458.
- Додано OCSP-схеми та приклад коду — 68ddcaa.
- Парсинг і перевірка часу підпису, перевірка атрибутів PKCS#7 та від'єднаних
  підписів; виправлено шифрування/розшифрування повідомлень і завантажувач ключів
  — 85b0161, a88e88b, e7420f9, 863e751.

## 2014 — початкова реалізація

- Арифметика еліптичних кривих над GF(2ᵐ) і підпис ДСТУ 4145 — додавання/множення
  точок, підпис приватним ключем, PRNG зі sjcl — 6644f5f, 68cc429, f52e9d0.
- Підписи ДСТУ 4145 у «короткому» ASN.1-форматі — e45cdba, a38c042, 4dfc6a2.
- PKCS#7 / CMS: ASN.1-схеми (`ContentInfo`, `SignerInfo`), розшифрування
  повідомлень, транспортний енкодер і ефемерні ключі — 411857b, 950c7b0, 7f15eaa,
  0c30f61.
- Сертифікати: парсинг відкритого ключа із сертифіката, завантаження X.509,
  експорт даних сертифіката як dict — ed84bef, 50f2d02, 1403ad3.
- Стандартна крива PB_431, визначення keystore і початкові (неповні) схеми
  CMP/CRMF — 791bb99, cd663b5, 8cb24d1.
